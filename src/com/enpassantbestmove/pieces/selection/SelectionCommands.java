package com.enpassantbestmove.pieces.selection;

public enum SelectionCommands {
    DO_NOTHING(),
    SELECT(),
    DESELECT(),
    DESELECT_SELECT(),
    GOTO_DESELECT(),
    EN_PASSANT(),
    CASTLE()
}
